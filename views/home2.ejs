<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-sRIl4kxILFvY47J16cr9ZwB07vP4J8+LH7qKQnuqkuIAvNWLzeN8tE5YBujZqJLB" crossorigin="anonymous">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/js/bootstrap.bundle.min.js" integrity="sha384-FKyoEForCGlyvwx9Hj09JcYn3nv7wiPVlz7YYwJrWVcXK/BmnVDxM+D2scQbITxI" crossorigin="anonymous"></script>
    <title>Document</title>
</head>
<body class="text-center">
    <h1 class="bg-primary text-white p-5">Famous Quotes</h1>
    <%- include("partials/nav.ejs") %>

    <div class="container py-4">
        <div class="row justify-content-center">
            <div class="col-md-6">
                <div class="card mb-4 shadow-sm">
                    <div class="card-body">
                        <form id="searchByKeywordForm" action="/searchByKeyword" method="GET" class="mb-4">
                            <div class="mb-3">
                                <label for="keyword" class="form-label">Search by Keyword</label>
                                <input type="text" class="form-control" id="keyword" name="keyword" placeholder="Enter keyword">
                            </div>
                            <button id="keywordBtn" type="submit" class="btn btn-primary w-100">Search by Keyword</button>
                        </form>
                    </div>
                </div>

                <div class="card mb-4 shadow-sm">
                    <div class="card-body">
                        <form action="/searchByAuthor" method="GET" class="mb-4">
                            <div class="mb-3">
                                <label for="authorId" class="form-label">Search by Author</label>
                                <select class="form-select" id="authorId" name="authorId">
                                    <% for(let authorRow of authorRows){ %>
                                        <option value="<%= authorRow.authorId %>"><%= authorRow.firstName %> <%= authorRow.lastName %></option>
                                    <% } %>
                                </select>
                            </div>
                            <button type="submit" class="btn btn-primary w-100">Search by Author</button>
                        </form>
                    </div>
                </div>

                <div class="card mb-4 shadow-sm">
                    <div class="card-body">
                        <form action="/searchByCategory" method="GET" class="mb-4">
                            <div class="mb-3">
                                <label for="category" class="form-label">Search by Category</label>
                                <select class="form-select" id="category" name="category">
                                    <% for(let cRow of cRows){ %>
                                        <option value="<%= cRow.category %>"><%= cRow.category %></option>
                                    <% } %>
                                </select>
                            </div>
                            <button type="submit" class="btn btn-primary w-100">Search by Category</button>
                        </form>
                    </div>
                </div>

                <div class="card mb-4 shadow-sm">
                    <div class="card-body">
                        <form action="/searchByLike" method="GET" class="mb-4">
                            <div class="mb-3">
                                <label class="form-label">Search by Likes Range</label>
                                <div class="row g-2">
                                    <div class="col">
                                        <input type="number" class="form-control" name="minLikes" placeholder="Min Likes">
                                    </div>
                                    <div class="col">
                                        <input type="number" class="form-control" name="maxLikes" placeholder="Max Likes">
                                    </div>
                                </div>
                            </div>
                            <button type="submit" class="btn btn-primary w-100">Search by Likes</button>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    
    <script src="/js/home.js"></script>
</body>
</html>